{
  "name": "tumblehead",
  "version": "1.0.0",
  "description": "Tumblehead website with animated navbar",
  "main": "src/js/main.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "clean": "rm -rf dist",
    "build": "npm run clean && vite build",
    "build:loader": "node scripts/build.js",
    "build:all": "npm run build && npm run build:loader",
    "preview": "vite preview",
    "postbuild:all": "git add dist/* && git commit -m \"chore: update dist files\" || true && git push origin main || true"
  },
  "keywords": [
    "webflow",
    "animation",
    "navbar"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vitejs/plugin-legacy": "^5.0.0",
    "terser": "^5.37.0",
    "vite": "^5.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/studiostarum/tumblehead.git"
  },
  "private": true
}
